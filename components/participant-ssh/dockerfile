FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive


RUN apt-get update && apt-get install -y \
    openssh-server \
    nmap \
    tcpdump \
    tshark \
    python3 \
    python3-pip \
    python3-scapy \
    iproute2 \
    iputils-ping \
    netcat-openbsd \
    jq
#    && rm -rf /var/lib/apt/lists/*



COPY ./ /ws

RUN chmod +x ./ws/ssh-setup.sh
ENTRYPOINT ["./ws/ssh-setup.sh"]

# Опциональные Profinet-инструменты
# RUN pip3 install profinet  # или другие библиотеки по задаче

# Настройка SSH
# RUN mkdir /var/run/sshd && \
#    useradd -m -s /bin/bash ctf && \
#    echo "ctf:yourpassword" | chpasswd

# Безопасность: запрещаем root-логин
# RUN sed -i 's/#PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config && \
#    sed -i 's/#PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config

# EXPOSE 22

# CMD ["/usr/sbin/sshd", "-D"]
